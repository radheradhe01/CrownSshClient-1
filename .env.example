# ──────────────────────────────────────────────
# SELF-HOSTING MINIMAL CONFIGURATION
# ──────────────────────────────────────────────
# This template contains the MINIMUM REQUIRED variables
# for the application to run correctly in Docker.
#
# Because the application ships with an internal Nginx 
# reverse proxy, URLs (like frontend, backend, or WebSockets) 
# are correctly inferred automatically.
#
# Copy this file to `.env` and fill in your secrets.
#   cp .env.example .env
# ──────────────────────────────────────────────

# 1. Google OAuth Credentials 
# Create these at https://console.cloud.google.com/apis/credentials
# Ensure your "Authorized redirect URIs" contains:
#   https://[YOUR_DOMAIN]/api/auth/google/callback
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# 2. Database Connection String
# Must be a valid MongoDB URI (e.g., MongoDB Atlas)
mongo=mongodb+srv://username:password@your-cluster.mongodb.net/database_name

# 3. Application Security Secrets
# ENCRYPTION_KEY: Must be exactly 64 hex characters (32 bytes) for AES-256
# You can generate one via terminal: openssl rand -hex 32
ENCRYPTION_KEY=insert_64_character_hex_string_here

# SESSION_SECRET: Used to securely sign authentication cookies
# Generate a strong random string
SESSION_SECRET=insert_strong_random_secret_string_here

# VITE_REQUIRED_PIN: A simple numeric pin to restrict certain destructive actions
VITE_REQUIRED_PIN=676869

# 4. Global Security Settings
# ALLOWED_IPS: Comma-separated list of IP addresses that can access the app.
# Leave completely blank to allow ALL traffic from anywhere.
ALLOWED_IPS=
